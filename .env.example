# Razorpay — copy to .env.local and fill from https://dashboard.razorpay.com/app/keys
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# Optional: only needed if you do not use keyId from /api/create-payment (local tooling)
NEXT_PUBLIC_RAZORPAY_KEY_ID=
